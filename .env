# Telegram
TELEGRAM_BOT_TOKEN=
EDIT_THROTTLE_SECS=3
# Google OAuth (Device Flow client)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Device flow mode (add this line)
GOOGLE_OAUTH_MODE=device

# These are unused in device mode (you can delete or leave them)
BASE_URL=
OAUTH_REDIRECT_URI=

# FastAPI can be left running or turned off; not needed in device mode
WEB_HOST=0.0.0.0
WEB_PORT=8080

DATA_DIR=./data
